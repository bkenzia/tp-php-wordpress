{% extends "admin/home.html.twig" %}
{% block main %}
    <h2>{{product.name}}</h2>
    <p>la description de produit : {{product.description}}</p>
    <h3>le prix de produit : {{product.price}}</h3>
    <h4>la contité en stoque : {{product.stock}}</h4>
    <h4>catégorie : {{product.category.name}}</h4>
{% endblock %}